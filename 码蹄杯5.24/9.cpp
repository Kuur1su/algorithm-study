#include <bits/stdc++.h>

using i64 = long long;
using u64 = unsigned long long;
using u32 = unsigned;

using u128 = unsigned __int128;
using i128 = __int128;

constexpr int P = 1E7 + 9;

int main() {
	std::ios::sync_with_stdio(false);
	std::cin.tie(nullptr);

	i64 n, q;
	std::cin >> n >> q;

	while (q--) {
		int l, r;
		std::cin >> l >> r;

		
	}

	return 0;
}